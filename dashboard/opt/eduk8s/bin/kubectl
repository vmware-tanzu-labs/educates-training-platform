#!/bin/bash

if [ -z "$KUBECTL_VERSION" ]; then
    KUBECTL_VERSION=1.17
fi

if [ ! -x /opt/eduk8s/bin/kubectl-$KUBECTL_VERSION ]; then
    KUBECTL_VERSION=1.17
fi

exec /opt/eduk8s/bin/kubectl-$KUBECTL_VERSION "$@"
