#@ load("@ytt:data", "data")
#@ load("@ytt:json", "json")
#@ load("@ytt:md5", "md5")
#@ load("/00-package.star", "image_reference", "image_pull_policy")
#@ load("config.lib.yaml", "environment_variables")

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: session-manager
  namespace: #@ data.values.namespace.name
spec:
  replicas: 1
  selector:
    matchLabels:
      deployment: session-manager
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        deployment: session-manager
      annotations:
        training.educates.dev/environment-variables: #@ md5.sum(json.encode(environment_variables()))
    spec:
      serviceAccountName: session-manager
      containers:
      - name: operator
        #@ image = image_reference("session-manager")
        image: #@ image
        imagePullPolicy: #@ image_pull_policy(image)
        envFrom:
        - configMapRef:
            name: session-manager
        volumeMounts:
        - name: config
          mountPath: /opt/app-root/config/
      volumes:
      - name: config
        secret:
          secretName: #@ "{}-config".format(data.values.resourceNamePrefix)
