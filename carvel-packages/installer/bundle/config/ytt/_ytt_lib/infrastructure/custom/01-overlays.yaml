#@ load("@ytt:data", "data")
#@ load("@ytt:overlay", "overlay")

#@overlay/match-child-defaults missing_ok=True
clusterPackages:
  contour:
    settings:
      infraProvider: custom
  external-dns:
    settings:
      infraProvider: custom
