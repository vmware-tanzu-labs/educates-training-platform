#! This is a test scenario for the kind cluster without educates package but using kyverno as clusterSecurity
clusterPackages:
  educates:
    enabled: false
clusterInfrastructure:
  provider: "kind"
clusterSecurity:
  policyEngine: "kyverno"
clusterIngress:
  domain: "educates.example.com"
