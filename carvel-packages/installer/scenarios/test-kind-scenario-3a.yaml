#! kind using provided domain with local CA only for Educates
clusterInfrastructure:
  provider: "kind"
clusterSecurity:
  policyEngine: "kyverno"
clusterIngress:
  domain: "educates.example.com"
  caCertificateRef:
    namespace: "educates-secrets"
    name: "wildcard-ca"
